/** @jsx React.DOM */
var React = require('react');

var SDTGrid = React.createClass({
    render: function() {
        alert("render");
        return <h1>Hello </h1>;
    }
});

module.exports = SDTGrid;